# Environment Variables Template
# Copy this file to .env.local and fill in your values

# Google Gemini AI. Read only by the server-side /api/gemini route.
# Never prefix this with NEXT_PUBLIC_ or expose it in browser code.
GEMINI_API_KEY=your_server_only_api_key

# Next.js
NEXT_PUBLIC_SITE_URL=https://imi.ge

# Analytics (if using)
# NEXT_PUBLIC_GA_ID=your_google_analytics_id

# Email Service (if using)
# SMTP_HOST=smtp.example.com
# SMTP_PORT=587
# SMTP_USER=your_email@example.com
# SMTP_PASS=your_password

# Database (if using)
# DATABASE_URL=postgresql://user:password@localhost:5432/dbname

# Sanity CMS (required for blog, about, legal pages, navigation)
NEXT_PUBLIC_SANITY_PROJECT_ID=your_project_id
NEXT_PUBLIC_SANITY_DATASET=production
SANITY_READ_TOKEN=your_read_token
